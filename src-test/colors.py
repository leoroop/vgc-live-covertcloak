import numpy as np

white_min = np.array([0, 0, 210])          # White tone HP Box
white_max = np.array([179, 85, 255])       # White tone HP Box

green_min = np.array([35, 210, 200])       # green tone picking ready
green_max = np.array([55, 230, 220])       # green tone picking ready

yellow_min = np.array([0, 200, 225])       # yellow tone input command
yellow_max = np.array([30, 255, 255])       # yellow tone input command

# yellow_min = np.array([0, 100, 225])       # yellow tone target clicked
# yellow_max = np.array([30, 255, 255])       # yellow tone target clicked
